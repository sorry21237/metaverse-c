#ifndef CALL_EXAMPLE_H //if the macro of CALL_EXAMPLE_H is not defined, use the following code until meet the #endif
#define CALL_EXAMPLE_H // it will prevents the flie from being including twice

//input (filename.c) function
void call_exam(void);

#endif
